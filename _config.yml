theme: jekyll-theme-slate
title: C++ for big number (arbitrary precision arithmetic)
description: a c++ implementation of big number arithmetic ( bignum & bigint ), perform computation on very large numbers, big integers, c++ beyond 20 digits unsigned long long int.
