#include <iostream>
#include <vector>

#ifndef _MAKE_LIB
#include "../ubint.hpp"
#else
#include <ubint.hpp>
#endif

#include "mini-test.hpp"

int main() { START_TEST;

    // test variables
    
    
    // ASSERT_EQUALITY((NUM2%NUM1),NUM2,"30 NUM2%NUM1");

    #if defined(_BASE2_16)
        RESULT("UBINT BASE 2^16 base2, base8, base10, base16 constructor");
    #elif defined(_BASE2_32)
        RESULT("UBINT BASE 2^32 base2, base8, base10, base16 constructor");
    #elif defined(_BASE2_64)
        RESULT("UBINT BASE 2^64 base2, base8, base10, base16 constructor");
    #endif
}